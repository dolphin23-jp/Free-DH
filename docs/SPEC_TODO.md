# SPEC_TODO — 仕様上の未確定事項

## COMBAT_SPEC §2: `fork(seed, label)` のハッシュ方式

- **箇所**: `docs/COMBAT_SPEC.md` §2「fork = `hash(親シード文字列 + ":" + ラベル)`」
- **問題**: `hash` のアルゴリズム、文字エンコーディング、出力ビット幅が指定されていない。方式が変わると敵順・ドロップ・ショップ・戦闘内乱数の全固定シード結果が変わる。
- **暫定判断**: `${String(seed)}:${label}` に対する32-bit FNV-1aを使用する。文字列はJavaScriptのUTF-16コード単位として走査し、結果をunsigned 32-bit整数としてmulberry32へ渡す。
- **理由**: 外部依存がなく、ブラウザとNodeで同一結果になり、実装と固定ベクトルが小さいため。
- **今後の確認**: セーブ互換性やゴールデンテストを固定する前に、この方式を正式仕様として採用するか確認する。変更する場合はT02の固定ベクトルと後続の全ゴールデンテストを更新する。

## COMBAT_SPEC §5/§12: `damageReduction` の適用位置

- **箇所**: §5のダメージパイプラインには `damageReduction` が現れず、§12のパッシブ語彙にのみ定義されている。
- **問題**: 加算・乗算・会心・特効・端数処理・ブロックのどの段階で固定値軽減を差し引くかが未指定。適用位置により会心時や小数ダメージの結果が変わる。
- **暫定判断**: 特効倍率の適用後、端数処理の直前に固定値を減算し、0未満にはしない。その後、小数第1位に丸めてブロックまたはHPへ適用する。
- **理由**: 固定値軽減をブロックとは独立した被ダメージ軽減として扱え、会心・特効後の最終威力から一定値を引く挙動になるため。
- **今後の確認**: 正式な適用位置が決まった場合、`calculateDamage` とT04の固定テストを同時に更新する。

## COMBAT_SPEC §6: 火傷バッチの満了tick

- **箇所**: 火傷は「付与ごとに独立バッチ」「各3.0秒で消滅」、状態異常ダメージは整数秒に処理するとある。
- **問題**: 付与からちょうど3.0秒後が整数秒と一致した場合、そのtickのダメージを適用してから消滅するか、適用前に消滅するかが未指定。
- **暫定判断**: 整数秒の状態異常ダメージを先に処理し、そのtickのCD進行後に残り時間を減らす。新規付与は同tickでは時間を消費せず、各バッチは最大3回の整数秒ダメージを発生させる。
- **理由**: 付与時刻によって2回または3回へ揺れず、3秒バッチの期待ダメージを決定的に保てるため。
- **今後の確認**: 満了tickをダメージ前に消滅させる仕様へ変更する場合、火傷の期待値とT05の固定テストを更新する。

## COMBAT_SPEC §6: 同一実体の毒・火傷の処理順

- **箇所**: tick内ではPlayer→Enemyの順序が指定されているが、同一実体に毒と火傷が同時にある場合の順序は未指定。
- **問題**: 片方のダメージでHPが0になった場合、死亡判定が即時なのでイベントログと後続ダメージの有無が変わる。
- **暫定判断**: 同一実体では毒を先、火傷を後に処理し、各適用直後に終了判定する。
- **理由**: 固定順を設けてイベントログを決定論的にし、毒スタックを単一値、火傷を複数バッチとして扱う状態構造とも整合するため。
- **今後の確認**: 正式順序が決まった場合、状態異常イベントのゴールデンテストを更新する。

## COMBAT_SPEC §7: `hpBelow` の閾値データ表現

- **箇所**: §7は `hpBelow(閾値を下回った瞬間)` と定義するが、元のitems.jsonトリガー語彙には閾値フィールドがない。
- **問題**: 効果量の `value` とHP閾値を同時に表現できず、任意閾値のトリガーをデータから構築できない。
- **暫定判断**: トリガースキーマと解決済みトリガー入力に任意の `thresholdPercent` を追加し、省略時は50%とする。既存JSONの数値・レコードは変更しない。発火済みキーを戦闘状態へ保存し、同じトリガーは1戦闘に1回だけ発火する。
- **理由**: JSON互換性を保ったまま任意閾値を型・スキーマで検証でき、将来データにhpBelowが追加されても同じ経路を使えるため。
- **今後の確認**: `thresholdPercent` と省略時50%を正式仕様として採用するか、T08前に決定する。

## COMBAT_SPEC §7: 命中後トリガーの細部順序

- **箇所**: §5(g)は `onHit` の後に被弾側 `onDamaged` とするが、`onBlocked` と `hpBelow` の相対位置は未指定。
- **問題**: 反射・回復・閾値回復が同じ一撃で成立した場合、最終HPや決着の有無が順序で変わる。
- **暫定判断**: `onHit → onDamaged → onBlocked → hpBelow` の順とする。`onDamaged` はHPまたはブロックが1以上減った場合、`onBlocked` は全額をブロックした場合のみ発火する。
- **理由**: 仕様に明記されたonHit→onDamagedを維持し、完全防御の追加処理とHP閾値判定をその後に一意化できるため。
- **今後の確認**: 正式順序が決まった場合、T06のトリガーイベント列テストを更新する。

## COMBAT_SPEC §7: トリガー由来ダメージの会心・ブロック規則

- **箇所**: 連鎖深度1は規定されているが、`damage`/`reflect`トリガーが会心判定やブロックを通るかは未指定。
- **問題**: 通常ダメージパイプラインを全て通すと乱数消費順が増え、反射をTrueダメージにすると防御価値が変わる。
- **暫定判断**: トリガー由来ダメージは非会心の通常ダメージとしてブロック可能にする。乱数は消費せず、`triggersAllowed: false`として新たなonHit/onDamaged/onBlocked/hpBelowを発生させない。
- **理由**: 反射を防御可能に保ちつつ、連鎖深度1と戦闘内乱数の限定条件を確実に守れるため。
- **今後の確認**: 反射・トリガーダメージを別種別にする場合、ダメージ記録とゴールデンテストを更新する。

## COMBAT_SPEC §7: `onKill` の発火範囲

- **箇所**: `onKill` は定義されているが、撃破したアイテム自身だけか、カバン内の全onKillトリガーかが未指定。
- **問題**: 毒・火傷・反射による撃破では単一の攻撃アイテムへ帰属できない場合がある。
- **暫定判断**: 勝者側の未封印実体が持つonKillをグリッド順で全て1回発火し、その後battleWinを同順で解決する。
- **理由**: 状態異常や反射撃破でも一貫して処理でき、アフィックスの収奪効果を取りこぼさないため。
- **今後の確認**: 攻撃元限定へ変更する場合、状態異常・反射撃破時の帰属規則を併せて定義する。

## COMBAT_SPEC §12: 隣接由来効果の合成順

- **箇所**: §12は隣接効果の種類と戦闘開始時スナップショットを定義するが、同じ対象へ複数の隣接効果が付く場合のトリガー順と、`effectMult`・固定加算の合成順は未指定。
- **問題**: 複数の `onHitPoison / onHitHeal / onHitGold` が同時に付く場合はイベントログ順が変わり、`effectMult` と `blockFlat` が同じ対象へ付く場合は計算順により最終値が変わる。
- **暫定判断**: 対象自身のデータ/解決済みトリガーを先に置き、その後へ隣接効果元をグリッド順、各効果元内はJSON配列順で追加する。能動効果は基礎値へ合算済み `effectMult` を乗じた後、`blockFlat` を加算する。スタミナ倍率は合算し、最終消費を0未満にしない。
- **理由**: 配置から一意に順序を決定でき、乗算効果と固定加算の役割を分離しながら負のスタミナ消費を防げるため。
- **今後の確認**: 正式な効果合成順が決まった場合、T07の隣接スナップショットとイベント順テストを更新する。

## COMBAT_SPEC §8: `undeadSlayer` の対象分類

- **箇所**: §8は聖剣の `undeadSlayer` 倍率を定義するが、どの敵をアンデッドとして扱うかを指定していない。
- **問題**: 敵名やtraitから実装側で推測すると、新規敵追加時に判定漏れが起き、データ駆動の原則にも反する。
- **暫定判断**: 敵データへ任意の `tags` を追加し、スケルトン・ヴァンパイア・リッチに `undead` を付与する。聖剣は名前やIDではなくタグだけを参照する。
- **理由**: 現在の敵設定上アンデッドと自然に分類できる3種を明示し、将来の追加・変更をJSONだけで行えるため。
- **今後の確認**: ヴァンパイアを対象に含めるかを含め、正式なアンデッド一覧を確定する。確定後は `enemies.json` のタグとT08テストを更新する。

## COMBAT_SPEC §8: 複数の構造スペシャルと仮想複製の境界

- **箇所**: `sealAdjacent` と `duplicateAdjacent` の単体挙動は定義されているが、複数個を配置した場合、既に封印された候補、仮想複製が持つ構造スペシャル、周回永続効果の帰属は未指定。
- **問題**: 呪いの偶像同士が封印し合う場合や王家の宝箱が王家の宝箱・勇者の剣を複製する場合、再帰的複製や消滅する仮想実体への周回成長が発生し得る。
- **暫定判断**: `sealAdjacent` は物理実体をグリッド順で処理し、既に封印済みの候補を飛ばす。途中で封印された効果元は以後処理しない。`duplicateAdjacent` も未封印の物理実体だけが1回解決し、仮想実体は構造スペシャルを再実行しない。仮想の勇者の剣は独立した戦闘内コピーとして成長し、その増分は元実体へ戻さない。
- **理由**: 無限複製を防ぎ、封印優先と「独立CDを持つ仮想実体」を最小限の追加規則で両立できるため。
- **今後の確認**: 仮想実体のパッシブ・ドロップ運・周回成長を戦闘後報酬へ含めるか、T10/T12/T15の統合前に確定する。

## COMBAT_SPEC §3/§8: 整数秒の敵trait処理順

- **箇所**: §3は状態異常とサドンデスの順を固定する一方、§8の `hpRegen / blockRegen / enrage` が同じ整数秒のどこで解決されるかを指定していない。
- **問題**: 状態異常やサドンデスでHPが0になる秒に再生を先に行うかで生死が変わり、enrageが同秒の攻撃へ乗るかでも被ダメージが変わる。
- **暫定判断**: 状態異常 → サドンデス → `hpRegen / blockRegen / enrage`（traits配列順）とする。死亡した時点で後続traitは処理せず、enrageは同tickの敵行動へ適用する。
- **理由**: §3に明記された処理を優先し、死亡後の再生を避けつつ、整数秒に成立した累積強化を直後の行動へ一貫して反映できるため。
- **今後の確認**: traitを独立したtick段階として§3へ追記し、正式な相対順を確定する。

## COMBAT_SPEC §8: 野盗撃破報酬のデータ表現

- **箇所**: `goldSteal + flee` は撃破時に「奪った全額+8G」とするが、`fleeAfterHits.value` は逃走までの命中数に使われ、+8Gを表すフィールドがない。
- **問題**: +8Gをコードへ直書きすると、バランス数値はJSONのみというデータ駆動規約に反する。
- **暫定判断**: `fleeAfterHits` traitに任意の `killBonus` を追加し、野盗へ8を設定する。T09では奪われたGの返還とこのボーナスを戦闘差分へ記録し、通常敵報酬の付与はT10に委ねる。
- **理由**: 既存のtrait意味を保ち、将来の値調整をJSON差分だけで行えるため。
- **今後の確認**: 通常報酬 `enemy.gold` と撃破ボーナスの合算規則をT10実装時に明文化する。

## COMBAT_SPEC §7/§8: 棘の致死打への反射

- **箇所**: `thorns` は「攻撃を当てるたび3ダメージを反射」とする一方、§9は各ダメージ直後に敵死亡を判定する。
- **問題**: 敵を倒した致死打にも反射すると、サドンデス以外でも双方HPが0以下になり得て、§9の「同一処理内で双方が0以下になり得るのはサドンデスのみ」と矛盾する。
- **暫定判断**: traitによる棘は敵がrevive/phaseを含めて生存した通常攻撃にのみ反応し、致死打では発生しない。
- **理由**: §9の即時終了と同時死亡の限定を優先する最小影響の解釈であるため。
- **今後の確認**: 致死打にも反射させる場合の勝敗優先規則を定義する。

## COMBAT_SPEC §2/§7: 複数封印の相対順と抽選候補

- **箇所**: 呪術師と呪いの偶像の封印は全battleStartより前とされるが、両方がある場合の相対順、およびランダム封印が既に封印済みの実体を候補に含むかは未指定。
- **問題**: 相対順と候補集合により、実際に無効化されるアイテム数、複製候補、会心より前に消費される乱数列が変わる。
- **暫定判断**: 配置由来の `sealAdjacent` を先に確定し、次に敵traits配列順で未封印の物理実体から重複なしに `sealRandomItem` を解決する。その後に複製と隣接スナップショットを行う。候補がなければ乱数を消費しない。
- **理由**: 戦闘外でも表示される配置封印を先に確定し、敵封印が必ず可能な限り指定数の実体を無効化しつつ、仮想実体を抽選対象へ混ぜないため。
- **今後の確認**: 同一対象への重複封印を許す設計なら、抽選候補と乱数消費規則を正式仕様へ追記する。

## COMBAT_SPEC §11: イベントの `side` と識別子

- **箇所**: `damage` と `status` イベントの `side` が効果元・対象のどちらか、`seal.itemId` が定義ID・インスタンスIDのどちらかを指定していない。
- **問題**: UIがログだけで状態を再生する際、向きや同一アイテムの識別を推測すると二重実装になる。
- **暫定判断**: `damage/status` の `side` は効果を受ける対象側とし、`seal.itemId` には配置インスタンスIDを格納する。`activate` と報酬系の `sourceId` も同じインスタンスIDまたは敵IDを使う。
- **理由**: UIが対象側のHP・状態を直接更新でき、同じ定義アイテムを複数配置しても一意に再生できるため。
- **今後の確認**: フィールド名を `targetSide` / `instanceId` へ変更するか、§11へ意味を明記する。

## COMBAT_SPEC §9/§11: 通常撃破と野盗逃走のゴールド

- **箇所**: 勝利時処理にゴールド獲得がある一方、野盗のfleeは「勝利(部分報酬)」「通常ドロップあり」とだけ記載されている。
- **問題**: flee時にも `enemy.gold` を得るか、盗難G消失だけで通常ゴールドは得るかが未指定。
- **暫定判断**: 通常撃破ではbattleWin後に `enemy.gold` を付与する。野盗fleeではドロップのみを後続システムへ許可し、`enemy.gold` は付与せず盗難Gも返さない。撃破時は通常ゴールドに加えて盗難返還とtraitの `killBonus` を付与する。
- **理由**: 「部分報酬」と「盗まれたGは消失」を明確に区別し、撃破の価値を維持するため。
- **今後の確認**: flee時の通常ゴールド有無を正式決定し、T12/T15の報酬フローへ反映する。
